# TTS Gateway 部署环境变量配置模板
# 复制此文件并根据环境修改

# ============================================
# CPU 服务器 (前端) 配置
# ============================================
# 将下面的配置复制到 frontend/.env.production

# GPU 服务器的公网地址（前端直接调用）
NEXT_PUBLIC_API_URL=http://your-gpu-server.com:8080

# ============================================
# GPU 服务器 (后端) 配置
# ============================================
# 将下面的配置复制到 gateway/.env

# Gateway 服务配置
GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=8000

# Qwen3-TTS 后端配置
QWEN3_TTS_URL=http://127.0.0.1:8019
QWEN3_TTS_ENABLED=true

# IndexTTS 后端配置
INDEXTTS_URL=http://127.0.0.1:8081
INDEXTTS_ENABLED=true

# 日志级别
LOG_LEVEL=info
