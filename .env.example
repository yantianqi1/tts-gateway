# ============================================
# TTS Gateway 配置文件
# ============================================
# 复制此文件为 .env 并根据需要修改

# ============================================
# 前端配置 (NEXT_PUBLIC_ 前缀会同步到前端)
# ============================================

# 后端 API 地址
# 本地开发: http://localhost:8000
# 远程 GPU 服务器: http://your-gpu-server:8080
NEXT_PUBLIC_API_URL=http://localhost:8000

# LLM 配置 (用于 AI 语音对话功能)
# Ollama 本地: http://localhost:11434/v1
# OpenAI: https://api.openai.com/v1
NEXT_PUBLIC_LLM_BASE_URL=http://localhost:11434/v1
NEXT_PUBLIC_LLM_API_KEY=ollama
NEXT_PUBLIC_LLM_MODEL=qwen2.5:latest

# ============================================
# Gateway 后端配置
# ============================================

# 服务器配置
TTS_GATEWAY_HOST=0.0.0.0
TTS_GATEWAY_PORT=8000

# Mock 模式（无需 TTS 模型，用于前端开发测试）
TTS_GATEWAY_MOCK_MODE=false

# Qwen3-TTS 后端配置
TTS_GATEWAY_QWEN3_TTS_URL=http://localhost:8019
TTS_GATEWAY_QWEN3_TTS_TIMEOUT=60.0
TTS_GATEWAY_QWEN3_TTS_ENABLED=true

# IndexTTS 后端配置
TTS_GATEWAY_INDEXTTS_URL=http://localhost:8081
TTS_GATEWAY_INDEXTTS_TIMEOUT=120.0
TTS_GATEWAY_INDEXTTS_ENABLED=true

# 限流配置
TTS_GATEWAY_RATE_LIMIT_ENABLED=true
TTS_GATEWAY_RATE_LIMIT_REQUESTS_PER_MINUTE=60

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
TTS_GATEWAY_LOG_LEVEL=INFO

# ============================================
# 端口配置（用于 run.sh 脚本）
# ============================================

# 前端端口
FRONTEND_PORT=3098

# Gateway 端口
GATEWAY_PORT=8000

# Qwen3-TTS 端口
QWEN_TTS_PORT=8019

# IndexTTS 端口
INDEXTTS_PORT=8081

# ============================================
# TTS 后端服务目录配置（用于 run.sh 脚本）
# ============================================

# Qwen3-TTS 服务目录（默认为 tts-gateway 的同级目录）
# TTS_GATEWAY_QWEN_DIR=/path/to/qwen3-tts-server

# IndexTTS 服务目录（默认为 tts-gateway 的同级目录）
# TTS_GATEWAY_INDEXTTS_DIR=/path/to/index-tts

# TTS 服务基础目录（默认为 tts-gateway 的父目录）
# TTS_GATEWAY_BASE_DIR=/path/to/base
