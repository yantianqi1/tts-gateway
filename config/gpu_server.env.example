# ============================================
# TTS Gateway - GPU 服务器配置文件
# ============================================
# 使用方法:
#   1. 复制此文件: cp gpu_server.env.example .env
#   2. 修改配置项
#   3. 运行部署: ./deploy_gpu.sh deploy
# ============================================

# ============================================
# Gateway 配置
# ============================================

# Gateway 监听端口 (对外暴露的唯一端口)
TTS_GATEWAY_PORT=8080

# Gateway 监听地址
TTS_GATEWAY_HOST=0.0.0.0

# 日志级别: DEBUG, INFO, WARNING, ERROR
TTS_GATEWAY_LOG_LEVEL=INFO

# ============================================
# TTS 后端配置
# ============================================

# Qwen3-TTS 后端 (本地)
TTS_GATEWAY_QWEN3_TTS_URL=http://127.0.0.1:8019
TTS_GATEWAY_QWEN3_TTS_TIMEOUT=60.0
TTS_GATEWAY_QWEN3_TTS_ENABLED=true

# IndexTTS 后端 (本地)
TTS_GATEWAY_INDEXTTS_URL=http://127.0.0.1:8081
TTS_GATEWAY_INDEXTTS_TIMEOUT=120.0
TTS_GATEWAY_INDEXTTS_ENABLED=true

# ============================================
# 速率限制
# ============================================

TTS_GATEWAY_RATE_LIMIT_ENABLED=true
TTS_GATEWAY_RATE_LIMIT_REQUESTS_PER_MINUTE=60

# ============================================
# 代码仓库配置 (用于自动克隆)
# ============================================

# Qwen3-TTS 推理服务仓库
# QWEN_TTS_REPO=https://github.com/your-org/qwen3-tts-server.git

# IndexTTS 推理服务仓库
# INDEXTTS_REPO=https://github.com/your-org/index-tts.git

# ============================================
# 部署目录配置
# ============================================

# 部署根目录 (默认为脚本所在目录)
# TTS_DEPLOY_DIR=/opt/tts-gateway

# ============================================
# 启动超时配置 (秒)
# ============================================

# Qwen3-TTS 启动超时 (首次加载模型可能较慢)
TTS_GATEWAY_QWEN_STARTUP_TIMEOUT=300

# IndexTTS 启动超时
TTS_GATEWAY_INDEXTTS_STARTUP_TIMEOUT=300
