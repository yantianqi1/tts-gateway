# TTS Gateway 配置文件

server:
  host: "0.0.0.0"
  port: 8000

backends:
  qwen3_tts:
    enabled: true
    url: "http://localhost:8019"
    timeout: 60.0

  indextts:
    enabled: true
    url: "http://localhost:8080"
    timeout: 120.0

rate_limit:
  enabled: true
  requests_per_minute: 60
